/**
 * GenerateInvoicePage
 * Phase 1 stub — heading only.
 * Full invoice form + preview will be wired in Phase 2.
 */
export default function GenerateInvoicePage() {
  return (
    <div className="h-full flex items-center justify-center">
      <div className="text-center">
        <h1 className="text-3xl font-black text-[#0a1f44] mb-2">Generate Invoice</h1>
        <p className="text-gray-500 text-sm">Full form + live preview coming in Phase 2.</p>
      </div>
    </div>
  );
}
